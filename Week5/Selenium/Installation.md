# Selenium Installation

Install Selenium using pip.

```bash
pip install selenium
```

Install a browser driver such as ChromeDriver or EdgeDriver.

Verify installation:

```python
import selenium
print(selenium.__version__)
```
